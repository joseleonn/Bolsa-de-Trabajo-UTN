# Para el correcto funcionamiento del Backend.

En local.

Instalar Docker.
Crear un contenedor con la imagen oficial de RabbitMQ y dejarlo con las configuraciones predeterminadas ya que esta configurado para que apunte a localhost.
